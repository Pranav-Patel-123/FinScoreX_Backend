{
  "version": 2,
  "builds": [
      {
          "src": "api/main.py",
          "env": {
                    "PYTHON_VERSION": "3.10"
                  }
          "use": "@vercel/python"
      }
  ],
  "routes": [
      {
          "src": "/(.*)",
          "dest": "main.py"
      }
  ]
}
